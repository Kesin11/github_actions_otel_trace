services:
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.82.0
    command: ["--config=/test-config.yaml"]
    volumes:
      - ./test-config.yaml:/test-config.yaml:ro
    ports:
      - "4318:4318"   # OTLP http receiver
