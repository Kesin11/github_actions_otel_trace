{
  "name": "root",
  "version": "0.0.0",
  "private": "true",
  "description": "",
  "author": "kesin1202000@gmail.com",
  "license": "MIT",
  "engines": {
    "node": ">=v18.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "reference": "workspaces-to-typescript-project-references",
    "clean": "npm run clean -ws --if-present",
    "build": "npm run build -ws --if-presene",
    "build:clean": "npm run build:clean -ws --ifpresent",
    "test": "npm run test -ws --if-present"
  },
  "devDependencies": {
    "@monorepo-utils/workspaces-to-typescript-project-references": "^2.9.0"
  }
}
